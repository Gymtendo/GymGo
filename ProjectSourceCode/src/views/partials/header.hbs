<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
    <meta charset="UTF-8" />
    <meta
    name="viewport"
    content="width=device-width, initial-scale=1"
    />

    {{!-- Title should be inserted here. Can be a <title> tag, a little graphic, or a title partial --}}

    {{!-- The "mobile" variable should be true is the screen width is less than 769px --}}
    {{!-- Here, if the screen is not small enough to be considered "mobile," the navbar should be displayed in the header --}}
    {{#unless mobile}}
    {{> nav}}
    {{/unless}}

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">

</head>
<body class="h-100 d-flex flex-column">